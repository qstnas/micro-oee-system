import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'MicroOEE System',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
